{
  "name": "ielts-result-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Telegram bot for IELTS results",
  "scripts": {
    "dev": "pip install -r requirements.txt && playwright install chromium && python3 app.py",
    "start": "python3 app.py"
  }
}
