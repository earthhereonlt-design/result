{
  "name": "ielts-result-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Telegram bot for IELTS results",
  "scripts": {
    "dev": "pip3 install --no-cache-dir -r requirements.txt && python3 -m playwright install chromium && python3 app.py",
    "start": "python3 app.py"
  }
}
